// Bootstrap JS plugin (client-only)
import 'bootstrap/dist/js/bootstrap.bundle.min.js'

export default defineNuxtPlugin(() => {
  // No-op: loading the bundle attaches Bootstrap behavior to the DOM
})





